name: ${project.name}
description: ${project.description}
version: ${project.version}
main: com.djtmk.disableai.DisableAI
authors: [djtmk, IgniteDEV]
api-version: 1.21

commands:
  disableai:
    description: DisableAI info
    default: true
    usage: /disableai
    aliases: [ai, dai]

permissions:
  disableai.reload:
    default: op
    description: Reload plugin
  disableai.manage:
    default: op
    description: Add, remove and list disabled entities
